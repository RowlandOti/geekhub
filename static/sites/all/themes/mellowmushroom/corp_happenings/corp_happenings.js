$(document).ready(function() {

  
	/*
  // Listener for all 
  $('.store-dropdown').change(function () {
    var index = $(this)[0].selectedIndex * 1;
    var storeName = $(this)[0].value;
    if (index > 0) {
      window.location.href =  storeName;
      //console.log('/store/' + storeName);
      return false;
    }
  });
  */
  
  
  
  
  
});